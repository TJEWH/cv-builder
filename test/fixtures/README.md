`Inter-Regular.ttf` is a variable-font fixture for vector glyph subsetting and
PDF embedding regression tests. It is not imported by the app or shipped in
production builds. Downloads embed the user's selected webfont instead.
